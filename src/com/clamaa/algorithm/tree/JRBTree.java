package com.clamaa.algorithm.tree;

/**
 * Red-Black Tree
 * Rules like below:
 * <p>1. each node is either Red or Black.</p>
 * <p>2. root is always black.</p>
 * <p>3. if the node is red, its children must be all black.(not vise versa)</p>
 * <p>4. each way from root to leaf or empty sub-node has the same number of black nodes(also call it black height).</p>
 * User: clamaa
 * Date: 4/22/13
 */
public class JRBTree<T> {
}
